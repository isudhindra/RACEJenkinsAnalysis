"""Cross-cutting helpers shared by routes and the analysis pipeline.

Each module is small and pure where possible. Domain-specific code
belongs elsewhere (``routes/``, ``pipeline/``), not in ``lib/``.
"""
